/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "static.vecteezy.com",
      "firebasestorage.googleapis.com",
      "example.com",
    ],
  },
};

export default nextConfig;
